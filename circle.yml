dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install pandoc
    - pip install -t tools/ -r tools/requirements.txt
test:
#  pre:
#    - echo 0
  override:
    - echo 0
#  post:
#    - echo 0
